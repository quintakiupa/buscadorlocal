{% extends 'base.html.twig' %}

{% trans_default_domain 'AppBundle' %}

{% block body %}
    <h1>{{ 'comercio.titulo_2' | trans}}</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>{{ 'tablas.campo_7' | trans }}</th>
                <th>{{ 'tablas.campo_1' | trans }}</th>
                <th>{{ 'tablas.campo_8' | trans }}</th>
                <th>{{ 'tablas.campo_4' | trans }}</th>
                <th>E-mail</th>
                <th>Web</th>
                <th>{{ 'tablas.campo_11' | trans }}</th>
                <th>Editar</th>                
            </tr>
        </thead>
        <tbody>
        {% for comercio in comercios %}
            <tr>
                <td><a href="{{ path('comercio_show', { 'id': comercio.id }) }}">{{ comercio.id }}</a></td>
                <td>{{ comercio.posicion }}</td>
                <td>{{ comercio.nombre }}</td>
                <td>{{ comercio.direccion }}</td>
                <td>{{ comercio.telefono }}</td>
                <td>{{ comercio.email }}</td>
                <td>{{ comercio.web }}</td>
                {% if is_granted('ROLE_ADMIN') %}
                <td><a href="{{ path('comercio_show', { 'id': comercio.id }) }}"><i class="fa fa-eye" aria-hidden="true"></i></a></td>
                <td><a href="{{ path('comercio_edit', { 'id': comercio.id }) }}"><i class="fa fa-pencil-square-o"></i></a></td>                
                {% endif %}
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
    {% if is_granted('ROLE_ADMIN') %}
        <li>
            <a href="{{ path('comercio_new') }}">{{ 'comercio.titulo_3' | trans}}</a>
        </li>
    {% endif %}
    </ul>
{% endblock %}
